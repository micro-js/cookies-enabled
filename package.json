{
  "name": "@f/cookies-enabled",
  "description": "Check whether or not cookies are enabled",
  "repository": "git://github.com/micro-js/cookies-enabled.git",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-undefined": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
