{
  "name": "@micro-js/cookies-enabled",
  "description": "Check whether or not cookies are enabled",
  "repository": "git://github.com/micro-js/cookies-enabled.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/is-undefined": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
